import React from 'react';

const Button = () => {
  return (
    <button
      aria-label="Saiba Mais"
      className="btn btn-light-mode btn-hover-green  "
      id="startButton"
    >
     BTE PAPO
    </button>
  );
};

export default Button;
